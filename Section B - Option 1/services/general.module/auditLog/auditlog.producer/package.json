{
    "scripts": {
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "tsc",
        "prestart": "npm run build",
        "start": "node .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "name": "covidscreener-auditlog",
    "version": "1.0.12",
    "description": "This package is used for logging audit events throughout the CovidScreener system",
    "author": "Creativ360",
    "license": "ISC",
    "dependencies": {
        "covidscreener-logging": "^1.2.4",
        "covidscreener-messagebus": "^1.0.3",
        "guid": "0.0.12",
        "mongodb": "^3.5.3"
    },
    "devDependencies": {
        "@types/guid": "^1.0.0",
        "@types/mongodb": "^3.3.16"
    }
}
