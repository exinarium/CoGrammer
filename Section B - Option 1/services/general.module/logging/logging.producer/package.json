{
    "scripts": {
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "tsc",
        "prestart": "npm run build",
        "start": "node .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "name": "covidscreener-logging",
    "version": "1.2.4",
    "description": "This package is used for logging throughout the CovidScreener system",
    "author": "Creativ360",
    "license": "ISC",
    "dependencies": {
        "covidscreener-messagebus": "^1.0.3",
        "mongodb": "^3.5.3"
    },
    "devDependencies": {
        "@types/guid": "^1.0.0",
        "@types/mongodb": "^3.3.16"
    }
}
